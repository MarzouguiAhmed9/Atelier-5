<style>
    table {
        width: 100%;
        border-collapse: collapse;
        margin-bottom: 20px;
    }

    th, td {
        padding: 12px;
        text-align: left;
        border-bottom: 1px solid #ddd;
    }

    th {
        background-color: #f2f2f2;
        font-weight: bold;
    }

    tr:hover {
        background-color: #f5f5f5;
    }
</style>
<form method="GET" action="{{ path('autherbybookmaxmin', {'min': app.request.query.get('min')|default('default_value'), 'max': app.request.query.get('max')|default('default_value')}) }}">
    <input type="search" name="min">
    <input type="search" name="max" ">
    <input type="submit" value="search">
</form>
<p>delte auther with 0 books</p> <a href="{{path('delete0books')}}" >delete </a>

<table>
    <thead>
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Email</th>
            <th>nbbok</th>
        </tr>
    </thead>
    <tbody>
        {% for author in authers %}
            <tr>
                <td>{{ author.id }}</td>
                <td>{{ author.username }}</td>
                <td>{{ author.email }}</td>
                <td>{{ author.getNbBooks()}}</td>
                <td><a href="{{path('edit_auther',{'id':author.id})}}" >edit </a></td>
                <td><a href="{{path('delete_auther',{'id':author.id})}}" >delete </a></td>
                <td><a href="{{path('detail_control',{'id':author.id})}}" >show</a></td>
                <td><a href="{{path('listbook',{'id':author.id})}}" >listbook</a></td>
                <td><a href="{{path('autherbybook',{'id':author.id})}}" >autherbybook</a></td>


            </tr>
        {% endfor %}
    </tbody>
</table>

